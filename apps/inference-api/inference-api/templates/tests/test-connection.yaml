apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inference-api.fullname" . }}-test-connection"
  labels:
    {{- include "inference-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inference-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
