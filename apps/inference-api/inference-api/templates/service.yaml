apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-api.fullname" . }}
  labels:
    {{- include "inference-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inference-api.selectorLabels" . | nindent 4 }}
